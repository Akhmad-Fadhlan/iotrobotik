<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    
    # Cegah loop: Jika request belum mengarah ke folder public/, tulis ulang ke public/
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
